/**
 * KafiStock Design System - Central Export
 * Export tất cả design constants
 */

// Colors
export { default as Colors, Primary, Accent, Semantic, Neutral, Stock } from './colors';

// Spacing
export { default as Spacing, ComponentSpacing } from './spacing';

// Typography
export { 
  FontSizes, 
  FontWeights, 
  LineHeights, 
  Typography,
} from './Fonts';

// Shadows
export { default as Shadows } from './shadows';

// Border Radius
export { default as BorderRadius } from './borderRadius';

// Animations
export { 
  AnimationDuration, 
  AnimationEasing, 
  SpringConfig, 
  TimingConfig,
} from './animations';

// Components
export {
  Button,
  Input,
  Card,
  IconSize,
  TouchTarget,
  ListItem,
  Avatar,
  Badge,
  Chip,
} from './components';

// App constants
export const APP_NAME = 'KafiStock';
export const API_TIMEOUT = 10000;

